# Claude Code project notes — Nucleus

This is the marketplace repo for **Nucleus — BrightWay AI's curated marketplace of plugins, agents, and shared memory. Tagline: "The operating core for how AI-powered teams get work done."**

The actual plugin source code lives in separate GitHub repos under `BrightWayAI/*`; this repo only holds the catalog manifest (`.claude-plugin/marketplace.json`), the marketplace README, contribution docs, and cross-cutting design proposals.

**Renamed from `claude-plugins` → `nucleus` on 2026-05-12.** GitHub auto-redirects old URLs. Local directory at `~/lab-bench/nucleus/`.

**Catalog currently lists 9 plugins.** Started at 12, gained `writing-style` (renamed `voice` 2026-09-15) and `daily-brief`, lost `plan-tomorrow` (folded into daily-brief), gained `nucleus-router` (JARVIS-style natural-language front door), gained `relationships` while retiring `weekly-outreach` + `bizdev-outreach` (both consolidated into `relationships`, 2026-05-28), then retired `lead-engine` + `referral-engine` into `relationships` too (2026-09-15), then retired `project-setup` + `client-status` into the renamed `delivery` plugin (2026-09-15), then retired `nucleus-router` itself — replaced by a `chief-of-staff` agent (`/cos`) inside `core-ops` (2026-09-15). Net 9.

## Layout

```
.claude-plugin/marketplace.json   ← the catalog (9 plugins)
README.md                          ← user-facing marketplace overview
CONTRIBUTING.md                    ← contributor guide for any plugin
SECURITY.md / LICENSE              ← standard repo files
docs/
├── multi-agent-patterns.md        ← pattern guide for chaining subagents inside plugins
└── proposals/
    ├── ROADMAP.md                 ← the active roadmap (start here)
    ├── *.md                       ← OPEN / not-yet-built specs
    ├── shipped/                   ← archived specs (SHIPPED banner) + README index
    └── parked/                    ← deferred indefinitely, NOT on the roadmap (e.g. jarvis-app)
```

## When picking up work on plugins

**The roadmap lives at `docs/proposals/ROADMAP.md`.** Convention: **top-level `docs/proposals/*.md` = open / not-yet-built**; `shipped/` = done (each archived with a SHIPPED banner; see also each plugin's `CHANGELOG.md`); `parked/` = deferred indefinitely, not on the roadmap. Each proposal is a self-contained brief designed to be picked up cold.

Currently open (as of 2026-06-08):

- **`docs/proposals/sweep-heartbeat.md`** — every-3h heartbeat that mines the day's surfaces into staged proposals reviewed at `/end-day`. Not started; gated on dogfooding `/listen` + `/morning` first.
- **cleanup-pass-1 items E–G** (deferred from cortex v4.8.1) — mining-agent consolidation, `/end-day` decomposition, autonomy-slider coverage. Tracked in `ROADMAP.md`; spec in `shipped/cleanup-pass-1.md`.
- **Parked:** `docs/proposals/parked/jarvis-app.md` — standalone Tauri desktop app; revisit only on strong marketplace-demand signal.

## How plugins are organized

The marketplace contains 9 plugins, each in its own GitHub repo:

| Plugin | Repo | Purpose |
|---|---|---|
| claude-cortex | BrightWayAI/claude-cortex | Always-on memory + shared `/setup-identity` and `/setup-voice` |
| core-ops | BrightWayAI/core-ops | Natural-language `chief-of-staff` agent (`/cos`, replaces `nucleus-router`), pipeline analyst + forecast subagents, /diagnose, /log-agent-run, /agent-metrics, /register-schedules |
| relationships | BrightWayAI/relationships | Daily relationship cockpit. `/relationships` 3-bucket brief, `/draft-touchpoint` per-contact drafting, `/network-rebalance` quarterly tier review, plus the Apollo signal pipeline (`/pull-signals`, `/capture-signal`, `/connect-signal`, `/warm-signal`, `/draft-signal`, `/pre-call-brief`) and referral-ask templates. 17-template library + `relationships-director` subagent (mode: rank/research). Absorbs and supersedes the retired `weekly-outreach` + `bizdev-outreach` (2026-05-28) and `lead-engine` + `referral-engine` (2026-09-15) plugins. |
| news-curator | BrightWayAI/news-curator | Weekly LinkedIn AI roundup (news-curator + post-assembler subagents) |
| delivery | BrightWayAI/delivery | Client engagement lifecycle: start, status, QA. New-engagement initialization, weekly client-status drafts, deliverable QA. Renamed from `project-setup` 2026-09-15 (GitHub repo renamed in place; history preserved, old URLs redirect) — absorbs the retired `client-status` plugin and core-ops's `/review-deliverable`. |
| time-tracking | BrightWayAI/time-tracking | Calendar → time-log → invoices |
| weekly-alignment | BrightWayAI/weekly-alignment | Slack cross-team alignment scanner |
| voice | BrightWayAI/voice | Adaptive voice learning — drafts, edit-detection, pattern-based style-guide refinement. Renamed from `writing-style` 2026-09-15 (GitHub repo renamed in place; history preserved, old URLs redirect). |
| daily-brief | BrightWayAI/daily-brief | Daily flow plugin. /brief = today's working surface (Cowork artifact); /process-brief acts on annotations; /brief --tomorrow blocks the next business day (/plan-tomorrow is a direct alias). As of v0.2.0 absorbs the deprecated plan-tomorrow plugin; as of 2026-09-15, /brief --tomorrow is the documented entry point. |

**Archived (retired) repos under BrightWayAI/:** `weekly-outreach`, `Biz-Dev` (deprecated 2026-05-28, redirect READMEs pointing at `relationships`, local clones removed). `lead-engine`, `referral-engine` (deprecated + archived 2026-09-15, redirect READMEs pointing at `relationships`, local clones removed). `client-status` (deprecated + archived 2026-09-15, redirect README pointing at `delivery`, local clone removed) — `project-setup` itself was renamed in place to `delivery`, not archived. `nucleus-router` (deprecated + archived 2026-09-15, redirect README pointing at `core-ops`'s `chief-of-staff` agent, local clone removed).

Each plugin has its own repo with `commands/`, `skills/`, `agents/`, `references/`, `CHANGELOG.md`, `SECURITY.md`, `LICENSE`. Local dev clones live as siblings to this marketplace repo in `~/lab-bench/`.

## Config-root convention

All plugins read user-specific config from `<config-root>/`, where `<config-root>` is the path stored in `~/Documents/.claude-plugin-config-root` (a single-line text file in the user's home directory, populated on first plugin setup). Layout:

```
<config-root>/
├── identity.md                                    (cortex /setup-identity)
├── voice.md                                       (cortex /setup-voice)
├── memory/                                        (cortex working memory)
├── plugins/
│   └── <plugin>.user-context.md                   (per-plugin config)
├── archive/YYYY-MM-DD/                            (raw daily archive — proposed in second-brain-extension)
├── daily-briefs/YYYY-MM-DD.md                     (interactive daily working doc — proposed)
└── time-log.csv                                   (time-tracking plugin)
```

This is the canonical write location for plugin runtime data. Don't write plugin runtime state anywhere else.

## Versioning

- Plugins are individually versioned in their own `plugin.json` (the authoritative source).
- **As of 2026-06-08, `marketplace.json` records each plugin's `version` for at-a-glance reference**, mirrored in the README "Plugin versions" table. Cowork still resolves the latest from each plugin's GitHub repo on next pull — these mirrors are for visibility, not resolution, so **keep them in sync when bumping a plugin** (update `plugin.json` → `marketplace.json` entry → README table).
- Bump pattern: minor for new commands/agents, patch for fixes or non-breaking refactors, major for breaking API or config-layout changes.
- Coordinated ecosystem releases are defined once under `releases/<release-id>/release.json`.
  Follow `docs/RELEASING.md`; do not create a Claude-only release decision. The native
  OpenAI catalog pins commits directly, while Claude uses the same version set and
  release contract.
- Live connector certification uses Core Ops `/test-connectors` and the shared plan in
  `connector-tests/plan.json`. Claude must run its own live probes because its connector
  authorization is host-specific, but the workflow and report schema are not duplicated.

## Nucleus as JARVIS — shipped (2026-05-16)

Phases 1 and 2 are live. Phase 3 (productization) is deferred until dogfooding.

### Phase 1 — Front door + memory legibility

- **`nucleus-router` v0.1.1** — Shipped at `https://github.com/BrightWayAI/nucleus-router`. Always-loaded skill matches natural-language utterances to the right Nucleus command and asks for confirmation. No more memorizing 57 commands. Intent table covers all 13 plugins; v0.1.1 added rows for cortex v4.5 commands. Spec: `docs/proposals/nucleus-router.md`.

- **cortex v4.5.0** — Shipped at `https://github.com/BrightWayAI/claude-cortex`. Three additions:
  - `/reindex` + indexer skill — auto-maintained `memory/index.md` catalog (zero-LLM, deterministic). Runs from `/end-day` Step 5.5, `/cleanup` Step 4.5, and on `/remember` writes via `.reindex-queue` marker.
  - `/research-gaps` + `gap-researcher` subagent + `/merge-research-draft` — autonomous gap-finder. Seven detection rules; web research with ≥2 independent sources; private-individual privacy rule; user-gated merge. Optional Step 5.5 of `/end-week`.
  - Spec: `docs/proposals/cortex-v4.5-legibility.md`.

### Phase 2 — Human UI substrate

- **`/setup-obsidian` (cortex v4.5.0)** — Config-only scaffolding for `.obsidian/` workspace + `VAULT.md` home page over `<config-root>/`. Idempotent and non-destructive. Daily-brief snapshots become Obsidian daily notes via folder config — zero plugin code change. Mobile-readable via Obsidian's iOS/Android apps + iCloud or Obsidian Sync. Spec: `docs/proposals/obsidian-as-ui.md`.

### Phase 3 — Productization (shipped 2026-05-16, same session)

Strategic decisions locked:
- **Monetization:** free OSS, MIT-licensed everywhere; BrightWay AI sells consulting / setup / customization / training on top. Nucleus is the lead magnet.
- **Audience:** solo operators / fractional consultants. Not general SMBs, not enterprise teams.
- **Vs Anthropic SMB bundle:** position purely on Nucleus's own merits; do **not** mention Anthropic SMB in public docs. Internal competitive context lives in `docs/proposals/smb-connector-audit.md`.

Deliverables:
- `docs/proposals/smb-connector-audit.md` — internal-only audit of connector parity vs Anthropic's Claude for Small Business bundle. Confirms Nucleus differentiates on memory + voice + relationships + daily flow; defers finance/contracts/content/payments to Anthropic.
- `README.md` — production-quality rewrite. JARVIS-forward, talk-don't-memorize hero, full catalog organized by domain, install in 5 min, Obsidian-as-UI section, recommended install combos, daily/weekly rhythm, who-this-is-for, customization, BrightWay services contact at the bottom.
- `docs/proposals/productization.md` — internal strategy doc. ICP profile, monetization, BrightWay services menu (setup-in-a-day / custom plugin dev / memory-voice training / team rollout / retainer), onboarding flow, demo script (8 min), distribution channels (direct outbound > content > ecosystem visibility), what's explicitly NOT in scope.

Open items for future sessions: optional `/onboarding` chained command, marketplace.json description polishing pass, 6-minute demo screen recording.

### How to use the JARVIS flow

1. Speak naturally to Claude with cortex + core-ops installed.
2. The `chief-of-staff` agent (`/cos`) suggests the right command; user confirms.
3. Output writes to `<config-root>/` markdown files.
4. Open `<config-root>/` in Obsidian to see the graph view; same files visible on phone.

## Recently completed work

- **Nucleus Operating Model Refactor — Phases 1–2 shipped, Phase 3 underway (2026-09-15):** brief at `~/Documents/ClaudeCortex/Projects/Nucleus/Nucleus_Operating_Model_Refactor_Brief_2026-09-15.md`. Refactors Nucleus from a personal-ritual second brain toward a scheduled-agent-first operating system. **Phase 1:** cleaned up legacy plugin configs (archived weekly-outreach/bizdev-outreach configs, deleted a stale duplicate brightway-core config); added the memory ownership contract + autonomy policy to `memory/CLAUDE.md`; instantiated core-ops's schedule library with a `nightly-listen` entry; demoted `/end-day` to optional and ported its tomorrow-seeding logic into `/morning` (now the required daily touch); merged duplicate Corrections sections in `user.md`. **Phase 2:** added Current-state/Open-loops/Record summary-layer structure to the six largest memory nodes (two split into sibling `.record.md` files); `/recall` now defaults to loading only Current-state+Open-loops on restructured nodes; new deterministic `cortex_cli.py check-caps` enforces the 150/200-line caps, wired into `/reindex`, `/cleanup`, `/diagnose`; created a private `memory/me/` scope (identity, voice, user, reflections, surfacing-prefs, style-eval-guide) excluded from the shared remote, plus a private `BrightWayAI/nucleus-memory` GitHub repo that `/morning` now commits+pushes to automatically; drained the 87-entry rehearse backlog and folded a weekly rehearsal batch into `/morning` on Fridays. **Also fixed in the same session:** a marketplace-wide privacy audit found and genericized real client/personal names hardcoded as "examples" in cortex, relationships, and writing-style docs (two sweeps — the first missed several, caught in a follow-up). **Phase 3 (in progress):** `writing-style` renamed to `voice` (GitHub repo renamed in place, history preserved) — `/setup-voice` deliberately stays in cortex rather than moving to the new plugin, since 9+ other plugins treat it as a foundational always-installed step. Then `relationships` absorbed `lead-engine` (Apollo signal pipeline: `/pull-signals`/`/capture-signal`/`/connect-signal`/`/warm-signal`/`/draft-signal`/`/pre-call-brief`, kept as separate commands rather than one flagged mega-command to preserve real per-touch nuance; `contact-researcher` agent moved over) and `referral-engine` (referral-ask templates folded into `/draft-touchpoint`; cooling rules native to `/setup-relationships`) — both plugins retired 2026-09-15, catalog now 11 plugins. `lead-engine`/`referral-engine` archived on GitHub with redirect READMEs (matching the `weekly-outreach`/`Biz-Dev` precedent); local clones removed. Then `project-setup` renamed in place to `delivery` (GitHub repo renamed, history preserved) and absorbed `client-status` (unchanged `/client-status` + `/setup-status`) and core-ops's `/review-deliverable` (unchanged; still reads brand/CRM config cross-plugin from `core-ops.user-context.md`) — `client-status` retired 2026-09-15 with a redirect README and archived on GitHub, local clone removed. Catalog now 10 plugins. Then, per Zach's explicit call after flagging a conflict (§3.1's target 13→6 architecture would have moved `/setup-voice` out of cortex and retired `time-tracking`/`weekly-alignment`, both of which contradict decisions Zach already made this session) — kept voice/time-tracking/weekly-alignment as-is, but proceeded with 3.1's chief-of-staff agent item: wrote `core-ops/agents/chief-of-staff.md` as a job description (purpose/goals/success/failure/escalation/tools, no inline procedure) that loads `hot.md`+`index.md`, reads the autonomy policy, and routes natural-language/role-addressed requests to installed commands; added `/cos` + always-loaded `skills/cos/SKILL.md`. Retired `nucleus-router` entirely (redirect README, archived on GitHub, local clone removed) — routing docs in `relationships` skills and cortex's `references/autonomy.md` updated to point at `/cos`. Catalog now 9 plugins. **Step 3.6** (agent job descriptions): scoped down from the brief's literal ask after checking real dependencies — kept core-ops's `pipeline-analyst`/`pipeline-forecast` in place rather than moving them into `relationships-director`, since `daily-brief`/`delivery`/`time-tracking`/`relationships` all depend on them living in `core-ops`. Merged `relationship-ranker`+`contact-researcher` into `relationships/agents/relationships-director.md` (mode: rank/research — zero cross-plugin blast radius). Merged cortex's `transcript-reviewer`+`conversation-miner`+`activity-miner` into `agents/note-taker.md` (mode: transcript/conversation/activity — they already ran together at the same `/listen`/`/end-day` pipeline step). Extracted a new `weekly-alignment/agents/alignment-scanner.md` (mode: scan/pulse/report) from that plugin's previously-inline Slack-reading logic, at Zach's explicit request after confirming it had zero existing subagents despite doing `pipeline-analyst`-shaped multi-channel synthesis work. Deliberately did NOT invent `delivery-director`/`ops-director` agents from the brief's target org chart — neither plugin has an existing agent or real multi-step synthesis need. **Step 3.5** (command folding): `/note`+`/learn` folded into `/remember --quick`/`--knowledge <type>` in cortex — kept as thin redirect aliases (not deleted) so existing invocations/schedules keep working; fixed a pre-existing hardcoded-path bug in `/learn` along the way. `/plan-tomorrow` folded into `/brief --tomorrow` in daily-brief via a mode-dispatch (not a full merge — the two commands have genuinely different mechanisms, read-only artifact render vs. calendar-event writes — so `plan-tomorrow.md` stays canonical and `/plan-tomorrow` remains a direct alias); fixed the same class of hardcoded-path bug there too. **Step 3.7** (skill auto-invocation audit): marked `disable-model-invocation: true` on every ritual or side-effecting skill across all 9 plugins (setup interviews, `/cleanup`/`/forget`/`/end-day`/`/end-week`/`/listen`/`/morning`/`/rehearse`/`/reindex`/maintenance skills in cortex; scheduling/telemetry/connector-probing in core-ops; signal-pipeline write steps and network-rebalance in relationships; weekly roundup/scan/pulse in news-curator/weekly-alignment; invoice generation in time-tracking; project-setup/plan-tomorrow in delivery/daily-brief) — 54 skills disabled, leaving 27 model-invocable (read-mostly, low-stakes, or high-frequency conversational skills like `/recall`, `/cos`, `/diagnose`, `/relationships`, `/brief`, `/style`, `/track-time`), under the brief's ≤30 target. All 9 plugins version-bumped, changelogged, validated, pushed. **Follow-up fix:** caught and reverted an over-eager disable on cortex's `morning` skill — `/morning` is the required daily touch specifically designed to fire on "good morning" phrasing, so it needed to stay model-invocable (now 28). **Step 3.8** (golden-thread archival filter) DEFERRED per Zach — requires evidence of which skills he's actually used repeatedly, and no usage telemetry exists to audit against; revisit once there's real usage history. **Phase 3 substantively complete.** **Phase 4 (in progress):** flagged and skipped two conflicts before starting — 4.1's literal `team/<slug>/` scope-creation step re-invokes the org/team physical-layout question already resolved in Phase 2 (no second teammate yet, so skipped); 4.3 (drop desktop Cowork artifacts from `/brief`/`/morning`, hosted-only) was flagged as too risky to rip out a battle-tested mechanism as a speculative step — Zach agreed, skipped for now. **4.1 shipped:** `/start-nucleus` gained a Step 2.5 autonomy-policy acknowledgment (reads the ALWAYS/ASK FIRST/NEVER tiers from `memory/CLAUDE.md`, lets the user accept or customize, writes a `.autonomy-acknowledged` marker) and its Step 5 per-plugin table + Step 8 closing summary were fixed — both still referenced retired plugins (`lead-engine`, `referral-engine`, `client-status`, `project-setup`) and the retired `nucleus-router`. **4.2 shipped:** added a `claude plugin eval` suite (confirmed the real schema via `claude plugin eval init --bare` rather than guessing) to the 5 commands a new user hits first — `/morning` (cortex), `/brief` (daily-brief), `/relationships`, `/client-status` (delivery), and natural-language → chief-of-staff (core-ops) — each an `llm`-graded case checking the skill/agent fires on natural phrasing without the user typing the explicit command; `--ablation with-without` reports the delta. **4.4 shipped:** added an "Autonomy and safety" section to `nucleus/README.md` (the one thing missing for a non-builder evaluating trust/safety) and fixed the stale "Agents and connectors" section, which still named `contact-researcher`/`relationship-ranker` as separate agents post-merge.

- **Brief-state bridge fix — daily-brief v0.6.1 + cortex v4.13.2 (2026-07-07):** root-caused why `/end-day` never saw brief annotations. Inspected the Cowork desktop app bundle (`coworkArtifact.js` preload + main-process handlers): artifacts get `window.cowork` = `{callMcpTool, askClaude, runScheduledTask}` only; `callMcpTool` requires a fully-qualified `mcp__<server>__<tool>` name that was declared in the artifact's `mcp_tools` allowlist at `create_artifact`/`update_artifact` time and resolves to `{content, structuredContent, isError}` without throwing. The v0.6.0 mirror called nonexistent `fs__write_file` with no allowlist and no isError check → **zero `.state.json` files ever written**, failures invisible. Fixes: **(daily-brief 0.6.1)** `/brief` Step 3.0 resolves + verifies a filesystem MCP write tool (verify-write creates the day's `.state.json`, so even a zero-action day has one), declares it in `mcp_tools`, injects `{{FS_WRITE_TOOL}}`/`{{STATE_MIRROR_PATH}}` tokens; template checks `isError` and shows a persistent "auto-sync unavailable — click 🔄 Sync for end-day" banner when degraded; `/setup-brief` Section 7 walks adding a filesystem MCP server scoped to `<config-root>`; `/process-brief` gains a paste path. **(cortex 4.13.2)** `/end-day` Step 2c.0p paste path — user clicks 🔄 Sync, pastes blob, `/end-day` validates + writes the state file itself — sits before the multi-select fallback gate; staleness check on `last_interaction_at`. Contracts + version mirrors updated. Note: the artifact-sandbox findings (allowlist mechanics, no built-in fs access, debug-log flag off by default, localStorage recoverable from the app's leveldb partition as last resort) are documented in `docs/contracts.md` § state-mirror.

- **End-Day Routine Improvement Spec shipped — daily-brief v0.5.0 + cortex v4.13.0 (2026-06-08):** plugin-level wiring of the brief-as-mined-artifact + suppression-learning work Zach flagged during `/end-day` (was an interim hand-fix). Spec at `~/Documents/Claude/Projects/Nucleus/End-Day_Routine_Improvement_Spec.md`. **Part A (daily-brief):** canonical brief is now **5 fixed sections** — Center of Gravity / Calendar Block (visual timeline strip + written list with per-meeting notes) / Priority Tasks (richer per-row actions: done/delegate/skip/not_important/annotate, replacing the bare checkbox) / Outreach Queue (per-contact actions + optional bucket/value-add/signal tags) / Yesterday's Reflection. `/brief` filters tasks + outreach against `memory/surfacing-prefs.md` before render. localStorage contract bumped to **schema_version 0.5.0** (`tasks`/`annotations`/`outreach_actions`; breaking vs 0.4.x `tasks_checked`, with back-compat mapping). HTML template rebuilt from Zach's reference prototype. **Part B (cortex /end-day):** Step 0.7 per-source consent + cost gate (Today's Brief required & first); **Step 2c mines the brief** (task/outreach actions → memory write-backs + suppression learning into `surfacing-prefs.md` via not_important + repeat-ignore rule; runs in both modes); Step 2.9 learnings-first narrative then memories AND forgettings side by side; Steps 4.5/4.6 propose tomorrow's priorities/outreach individually (batchable) → `briefs/<tomorrow>.seed.json`; Step 4.2 longitudinal `memory/reflections.md` store. **Taxonomy consolidation (B.3):** `/remember` knowledge types collapsed to **Insight / Decision / Gotcha / Correction** (Model→`INSIGHT [mental-model]`, Lesson→`INSIGHT`, Recipe→`INSIGHT [recipe]`; legacy entries still read). New cortex templates `references/surfacing-prefs-template.md` + `references/reflections-template.md`. Open questions D.1–D.4 resolved with Zach (lean taxonomy, one batch cost card, optional outreach tags, single reflections.md). Contracts updated in `docs/contracts.md`. **Follow-up pass (same day):** brief template reconciled to the handoff spec-v2 reference (`todays-brief.reference-2026-06-09.html`) — JS `BLOCKS`-array timeline, v2 token scheme, tokenized cowork-meta, always-visible outreach category selects, Booked dropped from default render, `tasks_checked` back-compat mirror; **memory-as-git finished** (cortex v4.13.0 `/end-day` Step 5.8 init-offer + core-ops v0.3.2 `/diagnose` Step 1D health line — proposal moved to `shipped/`); End-Day spec filed to `docs/proposals/shipped/`. Committed on branch `end-day-routine-improvements` across daily-brief / claude-cortex / core-ops / nucleus; not yet pushed.

- **relationships plugin shipped + BD consolidation (2026-05-28):** new plugin `relationships` v0.1.0 shipped to `BrightWayAI/relationships`. Daily 3-bucket cockpit (new business / relationship building / network expansion), 17-template library, channel-aware drafting, `relationship-ranker` subagent, `/network-rebalance` for quarterly tier review on cortex person pages. Introduces additive `relationships:` YAML frontmatter namespace on cortex person pages (additive, non-breaking; treated as candidate cortex v4.12.0 schema bump). **Coordinated retirement:** `weekly-outreach` (BrightWayAI/weekly-outreach) and `bizdev-outreach` (BrightWayAI/Biz-Dev) both archived on GitHub with redirect READMEs pointing at relationships; marketplace entries removed; local clones deleted from `~/lab-bench/`. Marketplace catalog 14 → 13. Proposal at `docs/proposals/relationships-plugin.md`. Contracts at `docs/contracts.md`.

- **v0.2 refactor (2026-05-11):** all 12 plugins migrated from plugin-folder-relative paths to `<config-root>/plugins/` paths, eliminating writes to Cowork's read-only mount. See each plugin's CHANGELOG for the v0.2.0 / v0.2.1 entries.
- **Phase 0 platform-agnostic Step 0 (2026-05-12):** every `request_cowork_directory(...)` call in setup commands is wrapped in a Cowork/Claude-Code conditional so the same plugin source works in both runtimes. All 13 plugin patch versions bumped: cortex 4.1.3, weekly-alignment 1.4.3, writing-style 0.1.1, the rest 0.2.3.
- **Phase 1 daily-brief plugin (2026-05-12):** new plugin `daily-brief` v0.1.0 shipped — `/brief` builds a Cowork artifact "Today's Brief" with seven annotated sections; `/process-brief` reads the annotations and routes them (drafts/reschedules/talking-points/dismissals). Phase 1 of the second-brain v2 extension; spec at `docs/proposals/SECOND-BRAIN-V2-SPEC.md`.
- **Phases 3-6 second-brain v2 (2026-05-12):** Phase 2 (separate inbox-triage plugin) deliberately skipped — `daily-brief` keeps its direct Gmail fallback. Shipped: Phase 3 person pages (cortex 4.2.0 + 6 plugins → 0.2.4), Phase 4 cheap-tier commit triage (cortex), Phase 5 `/end-day` orchestration chain (cortex), Phase 6 orphan + duplicate-topic guardrails (cortex). All plugin code pushed to GitHub.
- **Known design note:** `daily-brief` and `plan-tomorrow` overlap significantly on data sources (calendar / CRM / inbox / cortex). Different verbs (`/brief` = today's working surface with annotations + `/process-brief` action loop; `/plan-tomorrow` = tomorrow's calendar blocks). Decision to keep both was deferred — revisit once both are in daily use.
- **cortex v4.3.0 — `/end-day` mining layer (2026-05-12):** three read-only agents at Step 2a (`transcript-reviewer` expanded to two output streams, `conversation-miner` new, `activity-miner` new — scoped to events). Source-agnostic via adapter pattern at `agents/lib/note-source-adapters.md` (Granola / Gemini / Fireflies / Otter / Notion / generic Drive / generic Gmail / custom). Step 2b unified review gate with high-confidence-only toggle, auto-expanded cross-refs, dismissal log. Domain-node Scope convention via one-time migration in `/end-day` Pre-chain B (generic detection, no hardcoded names). New `/setup-sources` command with mandatory adapter health-check. `code-miner` deferred. `[confirmed/recalled]` substrate tags added on knowledge entries.
- **cortex v4.4.0 — forgetting / decay layer (2026-05-12):** the other half of the bidirectional second-brain. Decay model with four states (Fresh / Stale / Dormant / Cold) driven by `[confirmed:...]` age. Defaults at `<config-root>/memory/.decay-config.md` (60/180/365 days; GOTCHA and RECIPE decay 1.5× slower; CORRECTION immune). Per-node `decay_profile: fast | normal | slow` front-matter override. `/recall` flags aging entries inline and offers recall-time triage (re-confirm / demote / archive). `/remember` runs concept-drift detection on new INSIGHT/MODEL/GOTCHA/LESSON writes (Haiku-tier) and prompts the user on supersede/keep-both/edit/skip. New `/rehearse` command + skill: active retention loop, picks 3-5 aging entries weekly via `/end-week` Step 3.5. `/cleanup` deepened with section I for dormant knowledge and section H for cooling/dormant/cold-archive person pages (auto-archive to `memory/person/archive/`). `memory-librarian` ranks Fresh higher and skips `## Demoted knowledge` by default. Content is never auto-deleted; every transition is user-gated.

- **cortex v4.5.0 — legibility upgrade: memory index + research-gaps + Obsidian (2026-05-16):** Phase 1 finish + Phase 2 of the Nucleus-as-JARVIS initiative. Three additions: (1) `/reindex` + indexer skill auto-maintains `<config-root>/memory/index.md` (zero-LLM, deterministic walk + decay classification; runs from `/end-day` Step 5.5, `/cleanup` Step 4.5, and via `/remember`'s `.reindex-queue` marker). (2) `/research-gaps` + new `gap-researcher` subagent + `/merge-research-draft` — seven gap-detection rules (thin entity, stale fact in active rotation, contradiction within a node, orphan, under-cited high-confidence claim, decision gap, sparse domain), web research with ≥2-source rule, private-individual privacy rule enforced, user-gated merge; optional Step 5.5 of `/end-week`. (3) `/setup-obsidian` + `references/obsidian-config-templates/` — idempotent config-only scaffolding for `.obsidian/` + `VAULT.md` over `<config-root>/`; daily-brief snapshots become Obsidian daily notes via folder config; no plugin code change. All shipped to `BrightWayAI/claude-cortex` main.

- **nucleus-router v0.1.0 + v0.1.1 (2026-05-16):** new standalone plugin shipped to `BrightWayAI/nucleus-router`. Always-loaded `route` skill translates natural-language utterances to the right Nucleus slash command and asks for confirmation. Suggest+confirm flow (never auto-dispatches). Initial table covers all 57 commands across 13 prior plugins; v0.1.1 added rows for cortex v4.5 commands. Catalog updated to 14 plugins. Pushed to `BrightWayAI/nucleus`.

- **daily-brief v0.3.0 + cortex v4.6.0 cleanup pass (2026-05-16):** real-user feedback dogfooding the JARVIS flow — `/brief` and `/end-day` felt like overhead because too many fields and steps fired with no actionable downstream. Cuts: (1) daily-brief drops Section 7 (end-of-day prompts), removes meeting annotation textareas (meetings become read-only context cards), hides empty sections in the artifact, hides Section 5 until `/process-brief` produces drafts; removes `add_talking_point` action from `/process-brief`. (2) cortex `/end-day` defaults to quick mode (Steps 3, 4, 5, 5.5, 6) — 30s-3 min; `/end-day --full` adds the heavy steps (1, 2, 2a, 2b) for transcript-heavy days; auto-offer prompt only fires when transcripts ≥ 2 or inbox ≥ 5. Step 4 reflection now writes `## Reflection` to today's brief markdown — the new contract between the two plugins. Both shipped to GitHub.

- **cortex v4.7.0 — overnight learning + hot.md (2026-05-16):** answers "how does this learn while I sleep?" Two new commands and a rolling cache. (1) `/listen` is the nightly autonomous ingest pipeline — pulls yesterday's calendar / Gmail / Slack / Drive / transcripts (via configured note-source adapters) into immutable `<config-root>/archive/YYYY-MM-DD/`. Runs `transcript-reviewer`, `conversation-miner`, `activity-miner` against the archive read-only. Stages all proposed memory commits to `<config-root>/memory/.commit-drafts/YYYY-MM-DD.md`. No user gates — designed for cron via `core-ops /register-schedules` at 11pm or 5am. Modes: default (yesterday), `--date`, `--backfill N`, `--remine` (re-mine existing archive without re-pulling), `--rewrite`. (2) `/morning` is the interactive merge — reads latest `.commit-drafts/`, walks each proposal (accept/reject/edit/defer/skip-remaining), runs v4.4 drift detection on accepts, refreshes `memory/index.md` + `memory/hot.md`, optionally chains into `/brief`. (3) `memory/hot.md` is a rolling 7-day cache — verbatim citations from active nodes (last-week reflections, active people, open threads, recent commitments), capped at 3K words. Loaded first by `/recall` auto-fire so every session opens warm. Refreshed by `/listen` nightly, `/morning` after merge, and `/end-day` Step 5.6. Inspired by Karpathy's LLM-wiki `raw/` immutable source pattern and `hot.md` warm-start. Cortex v4.4.0 → v4.7.0 across three sub-sessions today. Router v0.1.1 → v0.1.2 added intent rows for `/listen` and `/morning`.

- **Karpathy pattern survey + cortex v4.7.1 + router v0.1.3 (2026-05-16):** `docs/proposals/karpathy-pattern-survey.md` audits every Karpathy pattern (LLM wiki, autoresearch, Software 3.0 talk) against Nucleus's current state. Most major gaps closed by v4.5-4.7. **All three identified high-value gaps shipped same session as cortex v4.7.1 + router v0.1.3:** (A) `<config-root>/memory/log.md` — append-only chronological log, `## [YYYY-MM-DD HH:MM] <op> | <summary>` format, grep-friendly, written by /listen, /morning, /end-day, /end-week, /reindex, /research-gaps, /merge-research-draft, /cleanup, /rehearse. (B) `llms.txt` at nucleus repo root following [llmstxt.org](https://llmstxt.org/) — single-file agent onboarding entry point (Karpathy Software 3.0 pattern). (C) Per-command autonomy slider — `references/autonomy.md` defines three modes (auto / suggest / confirm) with defaults tuned to operational risk; user override via `<config-root>/plugins/cortex.user-context.md` `autonomy:` section. Router v0.1.3 consults the slider before suggesting confirmation (auto skips the prompt entirely; confirm adds emphasis). All shipped to GitHub.

- **cortex v4.7.2 + core-ops v0.3.1 (2026-05-16):** addresses three loose ends from v4.7.1: (1) **Privacy defaults.** New `references/gitignore-template.md` — canonical privacy gitignore + cloud-sync notes (iCloud / Dropbox / OneDrive / Google Drive don't honor `.gitignore`; recommended pattern is symlinking `archive/` to `~/.cache/nucleus/archive/`). `/setup-identity` Step 3.5 and `/listen` Step 0.5 defensively create `.gitignore` excluding `archive/` + `memory/.commit-drafts/` + `memory/.research-drafts/` etc. (2) **Autonomy slider wired into command-level gates** — `/forget` Step 3, `/cleanup` Step 3, `/setup-obsidian` Step 2 now consult the autonomy mode and skip the gate in `auto` mode. Previously only the router consulted autonomy; commands had hardcoded prompts. (3) **Log centralization** — new `skills/log-writer/SKILL.md` is the single source of truth for `memory/log.md` format. All 9 logging commands invoke it instead of inlining the format string. (4) core-ops v0.3.1 adds `nightly-listen` (daily 11pm) and `weekly-research-gaps` (Sat 9am) rows to `references/schedules.template.md` so `/register-schedules` registers the new cortex v4.7 + v4.5 schedules out of the box.

- **Chief-of-Staff evolution — cortex v4.9.0 + router v0.2.0 (2026-05-20):** The router becomes a real orchestrator with verbs as the primary user surface. Three big things: (1) **15-verb surface** in nucleus-router v0.2.0 — start day / catch me up / research / draft / capture / plan / track / review / status update / bill / what's on my plate / what's missing / clean up / close day / close week. Each verb routes by context, invokes specialists, narrates work instead of asking permission. Role-addressable fallback for direct agent invocation ("ask my Chief Financial Agent to bill this month"). Parallel tool use via Anthropic API native for independent operations. (2) **workstream node type** in cortex v4.9.0 — `<config-root>/memory/workstream/<slug>.md` for ongoing initiative pipelines that span projects/people/topics. New `/start-workstream` command. Decays slowly. memory-librarian, indexer, hot.md all integrated. (3) **DECISION knowledge entry type** — first-class memory entries for forward-looking commitments with required Revisit-when trigger field. `/remember` detects decision cues; `/cleanup` Section J scans triggers via Haiku-tier classifier; decays slowly like RECIPE. Together these address the user-named gap: "I don't think in 60 commands; I think in verbs and roles." See `docs/proposals/chief-of-staff-evolution.md`.

- **Cleanup pass 1 — cortex v4.8.1 (2026-05-20):** First deliberate "clean before extending" cycle. Unified eight scattered staged dotfiles (`memory/.commit-drafts/`, `.research-drafts/`, `.rehearse-queue.md`, etc.) under `memory/staged/` tree. New `/migrate-staged-substrates` command + `references/migrations.md` documents the marker-gated one-time migration pattern. `/end-day` Pre-chain C invokes migration if marker missing. Plus: `nucleus/docs/contracts.md` enumerates every cross-plugin file-format dependency. Plus: README "Start here" callout (nucleus-router + claude-cortex + core-ops = minimum viable). Specs deferred to future passes: mining agent consolidation, `/end-day` decomposition, autonomy slider coverage. See `docs/proposals/cleanup-pass-1.md`.

- **Memory-as-git proposal (2026-05-20):** `docs/proposals/memory-as-git.md` — Liu's "git diff as review surface for memory" pattern formalized. `<config-root>/memory/` becomes a git repo; `/end-day` Step 5.8 auto-commits the day's changes; `/morning` Step 0.5 surfaces `git diff yesterday..HEAD` as the review surface. Three privacy levels (local-only / private GitHub / self-hosted). NOT IMPLEMENTED — spec only.

- **Sweep heartbeat proposal (2026-05-20):** `docs/proposals/sweep-heartbeat.md` — first concrete heartbeat. Fires every 3h work hours, mines today's surfaces + in-progress Cowork conversations, dedups three ways, stages proposals to `memory/staged/heartbeat-drafts/<today>.md`, reviewed at `/end-day` Step 3.5 (mirrors `/morning` walking `/listen` drafts). Liu codex-maxxing pattern. ~1-2 weeks build. NOT STARTED — dogfood `/listen` + `/morning` first.

- **JARVIS / Operator app spec (2026-05-16):** `docs/proposals/jarvis-app.md` — full build spec for a Tauri desktop app on top of the Nucleus substrate. Voice-first cockpit, 21st.dev voice-powered-orb (ogl + GLSL shaders), Anthropic SDK with 4-breakpoint prompt caching, browser voice in v1 (Whisper + ElevenLabs in Pro tier), reads/writes the same `<config-root>/` as Cowork plugins — third cockpit on shared substrate. 12-week single-engineer build. $39/mo Pro tier opens SaaS revenue ladder. Recommended app name: "Operator" (avoids JARVIS/Marvel trademark). NOT STARTED — decision deferred until marketplace dogfooding produces real usage data.

- **cortex v4.8.0 + router v0.1.4 + README rewrite + pruning (2026-05-16):** the productization unblock. (1) **New `/start-nucleus` command** in cortex — the "I just installed Nucleus, now what" walker. Chains identity → voice → sources → Obsidian → per-plugin setups → diagnostics → optional schedule registration. Idempotent. Every step has a skip. Router routes "start nucleus", "let's get started", "set me up", "onboard me", "first time setup", "let's begin" → `/start-nucleus`. (2) **`/observe` pruned to skill-only.** `commands/observe.md` deleted; full spec consolidated into `skills/observe/SKILL.md` (the command file's own description said "NOT a user-facing command"). Router intent row removed. (3) **Shipped proposals archived.** Eight proposals moved to `docs/proposals/shipped/` with a README index. Top-level `docs/proposals/` is now clean for pending work. (4) **README rewrite.** Catalog reorganized by AI staff roles instead of plugin domain — Chief Knowledge Officer (cortex), Chief of Staff (router), Communications Director (writing-style), Executive Assistant (daily-brief), Head of Outbound / Account Executive / VP of Relationships / Head of Partnerships (BD team), Project Manager / Account Manager / Chief Financial Agent (delivery team), Chief Marketing Agent (news-curator), Chief Operating Officer / Cross-Team Liaison (ops team). "Install in five minutes" updated to lead with "start nucleus." "Customize for your firm" rewritten — default customization is automatic via `/setup-*` interviews writing to `<config-root>/plugins/<plugin>.user-context.md`; forking is rare/advanced (changing methodology, not content).
- **Nucleus visibility + consolidation (2026-05-12):** core-ops 0.2.3 → 0.3.0 — new `/nucleus-status` (terse text snapshot) and `/nucleus-dashboard` v1 (Cowork HTML artifact with 6 section cards: stack overview, this-week activity, memory health, outreach pipeline, time+invoicing, impact-loop placeholder). daily-brief 0.1.0 → 0.2.0 — absorbs the deprecated plan-tomorrow plugin (`/plan-tomorrow` + `/setup-plan` now live in daily-brief; user-context backward-compatible). Marketplace catalog count drops 14 → 13. Standalone plan-tomorrow repo deprecated with a redirect README and archived on GitHub.
