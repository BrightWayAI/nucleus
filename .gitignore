# Slack MCP local caches — never commit
.channels_cache_v2.json
.users_cache.json

# macOS metadata
.DS_Store
**/.DS_Store

# Live connector test inputs and reports may contain local paths or credentials.
connector-tests/*.local.json
connector-tests/reports/
__pycache__/
