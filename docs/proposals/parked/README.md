# Parked proposals

Deferred indefinitely — **not on the active roadmap**. These are real ideas kept for reference, but explicitly out of scope until something changes (usually: marketplace dogfooding produces demand, or a strategic decision flips). They are not "next up."

| File | What | Why parked |
|---|---|---|
| `jarvis-app.md` | Standalone Tauri "Operator" desktop app (voice-first cockpit over the `<config-root>` substrate, $39/mo Pro tier) | Separate product bet; revisit only on strong demand signal from marketplace use. |

To revive one, move it back to `docs/proposals/` and add it to `ROADMAP.md`.
